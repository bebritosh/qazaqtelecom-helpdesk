# 🚀 Быстрый старт

## Запуск проекта (3 команды)

```bash
# 1. Применить миграции
.venv\Scripts\python.exe manage.py migrate

# 2. Создать демо-данные
.venv\Scripts\python.exe manage.py create_init_data

# 3. Запустить сервер
.venv\Scripts\python.exe manage.py runserver
```

Откройте: **http://127.0.0.1:8000/**

## 👥 Демо-пользователи

| Логин    | Пароль   | Роль          |
|----------|----------|---------------|
| admin    | admin    | Администратор |
| operator | operator | Оператор      |
| client   | client   | Клиент        |

## 🎯 Что попробовать

### Клиент (client/client)
1. Главная → **Запустить спидтест**
2. Если скорость < 5 Mbps → Откроется модалка
3. **Начать чат** → Написать проблему
4. Прикрепить фото (кнопка 📎)
5. Голосовой ввод (кнопка 🎤)

### Оператор (operator/operator)
1. Главная → **Панель оператора**
2. Просмотр открытых заявок
3. Детали заявки → История чата
4. **Сгенерировать ответ** (AI-assistant)

### Админ (admin/admin)
1. Главная → **Дашборд**
2. Статистика:
   - Всего заявок
   - % авто-решённых
   - Среднее время ответа
   - График по категориям

## ⚙️ Настройка Gemini API

1. Получите ключ: https://makersuite.google.com/app/apikey
2. Создайте `.env`:
   ```env
   GEMINI_API_KEY=ваш-ключ-здесь
   ```
3. Перезапустите сервер

## 🎨 Особенности дизайна

- ✅ Современный градиентный UI
- ✅ Анимации и переходы
- ✅ Адаптивный дизайн
- ✅ Индикатор печати бота
- ✅ Превью изображений
- ✅ Спиннеры загрузки
- ✅ Шрифт Inter (Google Fonts)

## 🐛 Если что-то не работает

1. **Ошибка миграций**: Удалите `db.sqlite3` и запустите миграции заново
2. **Нет Python**: Используйте полный путь `.venv\Scripts\python.exe`
3. **Ошибка Gemini**: Проверьте API ключ в `.env`
4. **Статика не загружается**: Создайте папки `static/` и `media/`

## 📱 Тестирование на мобильных

Сервер доступен в локальной сети:
```bash
python manage.py runserver 0.0.0.0:8000
```

Откройте на телефоне: `http://[IP-вашего-ПК]:8000`
