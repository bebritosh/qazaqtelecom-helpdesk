# 🎉 Новые функции Centrium

## 1. Рейтинг ответов AI (👍👎)

### Описание
Пользователи могут оценивать каждый ответ AI как полезный или бесполезный.

### Как работает
- Под каждым ответом AI появляются кнопки **👍 Полезно** и **👎 Не помогло**
- При клике оценка сохраняется в базе данных
- При возврате в чат оценки отображаются
- Зелёная кнопка = полезно, красная = не помогло

### Для аналитики
Можно добавить в дашборд:
```python
# Процент полезных ответов
helpful_count = Message.objects.filter(is_bot=True, rating=1).count()
total_rated = Message.objects.filter(is_bot=True, rating__isnull=False).count()
success_rate = (helpful_count / total_rated * 100) if total_rated > 0 else 0
```

### API
- `POST /tickets/api/chat/rate/`
  ```json
  {
    "message_id": 123,
    "rating": 1  // 1 = полезно, -1 = не помогло
  }
  ```

---

## 2. Быстрые ответы (Quick Replies)

### Описание
Кнопки с частыми проблемами для быстрого начала диалога.

### Доступные кнопки
1. 🚫 **Не работает интернет**
2. 📺 **Проблема с ТВ**
3. 🧾 **Вопрос по счету**
4. 🐌 **Медленная скорость**
5. 📡 **Настройка роутера**

### Поведение
- Показываются только в **новом чате** (без истории)
- При клике текст автоматически вставляется и отправляется
- После первого сообщения кнопки скрываются

### Кастомизация
Редактируй `templates/core/chat.html`:
```html
<button type="button" class="btn btn-outline-primary quick-reply-btn" data-text="Ваш текст">
  <i class="bi bi-icon me-1"></i>Текст кнопки
</button>
```

---

## 3. Статус "печатает..."

### Описание
Визуальный индикатор, что AI обрабатывает запрос.

### Как работает
- Появляется сразу после отправки сообщения
- Показывает анимированные точки ⋯
- Текст: **"AI печатает..."**
- Исчезает когда приходит ответ

### Анимация
CSS анимация в `chat.js`:
```css
.typing-dots span {
  animation: typing 1.4s infinite;
}
@keyframes typing {
  0%, 60%, 100% { transform: translateY(0); }
  30% { transform: translateY(-10px); }
}
```

---

## 4. Поиск и фильтрация тикетов

### Описание
Мощная система поиска в панели оператора.

### Возможности

#### Поиск (в реальном времени)
- По **ID тикета** (например: `#123`)
- По **теме** (например: `интернет`)
- По **имени клиента**
- **Подсветка** найденного текста жёлтым

#### Фильтры
1. **Статус**: Все / Новые / В работе / Закрытые
2. **Категория**: Все / Интернет / ТВ / Биллинг
3. **Приоритет**: Все / Высокий / Средний / Низкий

#### Счётчик
- Обновляется в реальном времени
- Показывает количество **видимых** тикетов

#### Кнопка "Сброс"
- Очищает все фильтры
- Сбрасывает поиск
- Показывает все тикеты

### Технические детали
- Работает **на клиенте** (без перезагрузки страницы)
- Скрипт: `static/js/operator-search.js`
- Поддерживает **русский и казахский** языки

---

## 🚀 Как протестировать

### 1. Рейтинг ответов
```bash
# Открой чат
http://127.0.0.1:8000/chat/

# Задай вопрос AI
# Под ответом нажми 👍 или 👎
# Обнови страницу — оценка сохранится
```

### 2. Быстрые ответы
```bash
# Открой новый чат (очисти cookies или инкогнито)
http://127.0.0.1:8000/chat/

# Увидишь 5 кнопок с частыми проблемами
# Нажми любую — текст отправится автоматически
```

### 3. Статус "печатает..."
```bash
# В чате отправь любое сообщение
# Сразу увидишь "AI печатает..." с анимацией
# Через 1-3 секунды появится ответ
```

### 4. Поиск тикетов
```bash
# Войди как оператор
http://127.0.0.1:8000/login/
# Логин: operator / Пароль: operator

# Открой панель оператора
http://127.0.0.1:8000/tickets/operator/

# Попробуй:
# - Ввести "#1" в поиск
# - Выбрать статус "Новые"
# - Нажать "Сброс"
```

---

## 📊 Метрики для дашборда

### Рейтинг AI
```python
from tickets.models import Message

# Общая статистика
total_ai_messages = Message.objects.filter(is_bot=True).count()
rated_messages = Message.objects.filter(is_bot=True, rating__isnull=False).count()
helpful = Message.objects.filter(is_bot=True, rating=1).count()
not_helpful = Message.objects.filter(is_bot=True, rating=-1).count()

rating_percentage = (helpful / rated_messages * 100) if rated_messages > 0 else 0

print(f"AI Success Rate: {rating_percentage:.1f}%")
print(f"Rated: {rated_messages}/{total_ai_messages}")
```

### Популярные быстрые ответы
Можно логировать клики на кнопки и анализировать:
- Какие проблемы самые частые
- В какое время суток больше обращений
- Какие кнопки не используются (можно заменить)

---

## 🔧 Настройка

### Изменить текст быстрых ответов
Файл: `templates/core/chat.html`
```html
<button type="button" class="btn btn-outline-primary quick-reply-btn" 
        data-text="Новый текст для AI">
  <i class="bi bi-новая-иконка me-1"></i>
  Текст кнопки
</button>
```

### Добавить новую кнопку
```html
<button type="button" class="btn btn-outline-primary quick-reply-btn" 
        data-text="Проблема с модемом">
  <i class="bi bi-router me-1"></i>
  Проблема с модемом
</button>
```

### Изменить цвет кнопок оценки
Файл: `static/js/chat.js`
```javascript
// Полезно (зелёная)
helpfulBtn.className = 'btn btn-sm btn-success';

// Не помогло (красная)
notHelpfulBtn.className = 'btn btn-sm btn-danger';
```

---

## 🐛 Известные ограничения

1. **Рейтинг** — можно изменить оценку сколько угодно раз
2. **Быстрые ответы** — только 5 кнопок (можно добавить больше)
3. **Поиск** — работает только на текущей странице (без пагинации)
4. **Статус "печатает..."** — фиксированный текст (не зависит от языка пользователя)

---

## 💡 Идеи для улучшения

1. **Рейтинг**
   - Добавить комментарий к оценке
   - Показывать средний рейтинг AI в дашборде
   - Отправлять уведомление при низком рейтинге

2. **Быстрые ответы**
   - Динамические кнопки на основе истории
   - Персонализация под пользователя
   - A/B тестирование текстов

3. **Поиск**
   - Поиск по дате создания
   - Сохранение фильтров в localStorage
   - Экспорт результатов поиска

4. **Статус "печатает..."**
   - Показывать для оператора тоже
   - Добавить звук уведомления
   - Показывать имя оператора

---

## 📝 Changelog

### v1.1.0 (2025-12-05)
- ✅ Добавлен рейтинг ответов AI (👍👎)
- ✅ Добавлены быстрые ответы (5 кнопок)
- ✅ Улучшен индикатор "печатает..."
- ✅ Реализован поиск и фильтрация тикетов
- ✅ Подсветка найденного текста
- ✅ Счётчик видимых тикетов

---

## 🆘 Поддержка

Если что-то не работает:
1. Проверь консоль браузера (F12)
2. Проверь логи Django
3. Убедись, что миграции применены: `python manage.py migrate`
4. Очисти кэш браузера (Ctrl + F5)
